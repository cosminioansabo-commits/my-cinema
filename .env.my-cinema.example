# =============================================================================
# MY-CINEMA NAS DEPLOYMENT - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your values
# Used by: docker-compose.my-cinema.yml
# =============================================================================

# Your GitHub username (lowercase) - for pulling images from ghcr.io
GITHUB_USERNAME=your-github-username

# NAS IP address or domain (used for CORS and external URLs)
# For local access only:
NAS_IP=192.168.1.100
# For remote access via DDNS:
# NAS_IP=your-domain.example.com

# User/Group IDs - run `id username` on your NAS to find these
PUID=1000
PGID=1000

# Timezone
TZ=Europe/Bucharest

# =============================================================================
# TORRENT SERVICES
# =============================================================================

# qBittorrent credentials (set these in qBittorrent WebUI first)
QBITTORRENT_USERNAME=admin
QBITTORRENT_PASSWORD=your-qbittorrent-password

# Prowlarr API Key (found in Prowlarr -> Settings -> General -> API Key)
PROWLARR_API_KEY=your-prowlarr-api-key

# Search Providers (comma-separated: prowlarr, torrentio, yts)
# Use only prowlarr for private trackers
SEARCH_PROVIDERS=prowlarr

# =============================================================================
# MEDIA LIBRARY SERVICES
# =============================================================================

# Radarr API Key (found in Radarr -> Settings -> General -> API Key)
RADARR_API_KEY=your-radarr-api-key

# Sonarr API Key (found in Sonarr -> Settings -> General -> API Key)
SONARR_API_KEY=your-sonarr-api-key

# =============================================================================
# JELLYFIN (Required for video playback)
# =============================================================================

# Internal URL (container-to-container communication)
# Use container name when on same Docker network
JELLYFIN_URL=http://jellyfin:8096

# External URL (browser access for HLS streaming)
# Must be accessible from the user's browser
# For local: http://YOUR_NAS_IP:8096
# For remote: http://your-domain.example.com:8096
# IMPORTANT: Port 8096 must be forwarded on your router for remote playback
JELLYFIN_EXTERNAL_URL=http://192.168.1.100:8096

# Jellyfin API Key - generate in Jellyfin Dashboard -> API Keys -> Add
JELLYFIN_API_KEY=your-jellyfin-api-key

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Generate a random JWT secret (run: openssl rand -base64 32)
JWT_SECRET=your-random-32-character-secret-key

# Password hash - generate with:
#   cd server && npm run hash-password "your-secure-password"
# Or online: https://bcrypt-generator.com/ (use 10 rounds)
# Leave empty to disable authentication
APP_PASSWORD_HASH=$2b$10$your-bcrypt-hash-here

# =============================================================================
# EXTERNAL APIS (Frontend)
# =============================================================================

# OMDb API Key for Rotten Tomatoes/Metacritic scores
# Get a free key at: https://www.omdbapi.com/apikey.aspx
VITE_OMDB_API_KEY=your-omdb-api-key

# =============================================================================
# OPENSUBTITLES (Optional - for subtitle search)
# =============================================================================
# Register at https://www.opensubtitles.com/en/consumers
# Create an API Consumer to get your API key

# API Key (required for subtitle search)
OPENSUBTITLES_API_KEY=

# Username and password (required for downloading subtitles)
# Without these, you can search but not download
OPENSUBTITLES_USERNAME=
OPENSUBTITLES_PASSWORD=

# =============================================================================
# BACKEND EXTERNAL URL
# =============================================================================
# The publicly accessible URL of the backend API
# Used by HLS.js to fetch video segments through the proxy
# For local: http://YOUR_NAS_IP:3001
# For remote: http://your-domain.example.com:3001
BACKEND_EXTERNAL_URL=http://192.168.1.100:3001
